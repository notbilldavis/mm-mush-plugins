<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>

<muclient>

<plugin
   name="affects_buttons"
   author="Oona"
   id="3a8348c0204a529dc99c2ba8"
   language="Lua"
   purpose="clickable buttons list that changes colors for your affects"
   save_state="y"
   date_written="2025-03-16 13:57:37"
   date_modified="2025-07-15 10:45:00"
   requires="4.71"
   version="2.1"
   >

<description trim="y">

<![CDATA[

Adds a panel you can add buttons to that will perform commands when clicked.
Buttons with an affect associated will be color-coded based on its status
This affect is typically the name of the skill/spell in lower case
Buttons without an affect will just be gray.

You can right-click a button to perform a lot of actions and you can use the
following commands to things as well, such as add buttons.

 * affects add <title> a:<affect> c:<command>
 * affects edit <title> a:<affect> c:<command>
 * affects delete <title>

<title> is what the button will say
<affect> is the affect it will try to track, it can be empty
<command> is the command to send when clicked

Using 'affects add' without any arguments will walk you through it step by step
and using 'affects edit <title>' will walk you through it that way.

 * affects move up <title>
 * affects move down <title>

This will move the button around.

 * affects broadcast
 * affects broadcast <affect>

When this affect is applied we can broadcast it for other plugins to pick it up

  * affects configure

This will open up a configuration window that will let you do all sorts of things.

A lot of these can be done by right clicking as well, also right click to set an
option as a favorite, this will add a button to auto-cast every button you have
marked if it is not casted, about to expire, or doesn't have an affect.

Some notes:

Druid affects are usually translated to non-druid spells that are exclusive so 
having a ring that gives stone skin will still show your invoke ooze as casted

This isn't 100% accurate. Some spells don't fire off events when they expire so
to be certain you are up to date you can view your affects manually at anytime
which will update the buttons

When recasting something before it expires it will try to guess the duration
based on the highest duration it's been casted. This can get weird when you do
something like cast levitation on your horse. It will think it was casted on you
and reset your meter thingy. Manually check affects to fix it.

Examples:

affects add Whirlwind a:whirlwind c:cast whirlwind
affects add Invoke Ooze a:stone skin c:invoke ooze
affects add Summon Mount a: c:cast 'summon mount'


MAKE SURE YOU TURN 'SHOW-AFFECT-SPOILERS' ON IN-GAME!

]]>

</description>

</plugin>

<aliases>

  <alias match="^affects[ ]+add(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>AddButton("%1")</send>
  </alias>

  <alias match="^affects[ ]+edit(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>EditButton("%1")</send>
  </alias>

  <alias match="^affects[ ]+delete(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>DeleteButton("%1")</send>
  </alias>

  <alias match="^affects[ ]+rename(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RenameButton("%1", "%2")</send>
  </alias>

  <alias match="^affects[ ]+move(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>MoveButton("%1")</send>
  </alias>

  <alias match="^affects[ ]+(?:config|configure)\b(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>Configure("%1")</send>
  </alias>

  <alias match="^affects[ ]+broadcast(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>Broadcast("%1")</send>
  </alias>

  <alias match="^affects[ ]+setchar[ ]+(.*?)$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>SetCharacterName("%1")</send>
  </alias>

  <alias match="^affects[ ]+cast[ ]+favorites" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>CastFavorites()</send>
  </alias>
  
  <alias match="^affects[ ]+update$" enabled="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>CheckForUpdates()</send>
  </alias>

</aliases>

<triggers>

  <trigger match="^(|[^ ]+(.*?))(\<(.+)hp (.+)sp (.+)st\>|\(.+\)) $" enabled="y" name="first_prompt" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>OnFirstPrompt()</send>
  </trigger>

  <trigger match="^You are affected by the following\:$" enabled="y" name="start_affects" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>StartCapturingAffects()</send>
  </trigger>

  <trigger match="^Total of (.*?)magical(.*?) affects\.$" enabled="y" name="stop_affects" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>StopCapturingAffects()</send>
  </trigger>

  <trigger match="^(?:Spell|Affect)\:(.*?)\- Level (.*?)\, modifies (.*?) for (.*?) minute(.*?)$" enabled="y" group="get_affects" keep_evaluating="n" regexp="y" send_to="12" sequence="100">
    <send>SetAffect("%1", "%4", false)</send>
  </trigger>

  <trigger match="^(?:Spell|Affect|Disease|Prof)\:(.*?)\- Level (.*?)\, modifies (.*?)by(.*?).(.*?)$" enabled="y" group="get_affects" keep_evaluating="y" regexp="y" send_to="12" sequence="100">
    <send>SetAffect("%1", "-1", false)</send>
  </trigger>

  <trigger match="^You have increased the duration of the spell \'(.*?)\'\.$" enabled="y" keep_evaluating="n" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RecastAffect("%1")</send>
  </trigger>

  <trigger match="^\[Off\: You lose (.*?)\] (.*?)\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("%1")</send>
  </trigger>

  <trigger match="^You are surrounded by an ochre glow as the last vestiges of \'(.*?)\' are burned from your body\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("%1")</send>
  </trigger>
  
  <trigger match="^(.*?) knocks (.*?) from your grip\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>DisarmedBroadcast("%2")</send>
  </trigger> 

  <trigger match="^(.*?) knocks (.*?) from your grip\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>DisarmedBroadcast("%2")</send>
  </trigger> 

  <trigger match="^(.*?) powers deftly knock (.*?) from your grasp\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>DisarmedBroadcast("%2")</send>
  </trigger> 

  <trigger match="(.*?) batters you mercilessly before finally cleaving your (.*?) in twain\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>DisarmedBroadcast("%2")</send>
  </trigger> 

  <trigger match="^You call your familiar\, and a wary\-looking wolf appears right on cue to join you\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>SetAffect("wolf_familiar", "120", true)</send>
  </trigger>

  <trigger match="^There is a slight \*poof\* as an alert wolf vanishes from your inventory\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("wolf_familiar")</send>
  </trigger>

  <trigger match="^You feel relieved as the (plague|poison) is burned from your body\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("%1")</send>
  </trigger>

  <trigger match="^Your vision returns\!$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("blindness")</send>
  </trigger>

  <trigger match="^The shimmering bands of light surrounding you fade and vanish\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAffect("spell shield")</send>
  </trigger>

  <trigger match="^Your disembodied soul rises from your ravaged corpse\.$" enabled="y" keep_evaluating="y" regexp="y" send_to="12" sequence="100" group="affects_buttons">
    <send>RemoveAllAffects()</send>
  </trigger>

  <trigger match="^You are affected by the following\:$" enabled="n" keep_evaluating="y" regexp="y" send_to="14" sequence="100" omit_from_output="y" group="affects_buttons_clean">
    <send></send>
  </trigger>

  <trigger match="^(.*)$" enabled="n" keep_evaluating="y" regexp="y" send_to="14" sequence="100" omit_from_output="y" group="affects_buttons_clean">
    <send>CleanAffectLine("%1")</send>
  </trigger>

  <trigger match="^Total of .* affects%.$" enabled="n" keep_evaluating="y" regexp="y" send_to="14" sequence="100" omit_from_output="y" group="affects_buttons_clean">
    <send></send>
  </trigger>
</triggers>

<timers>

  <timer name="affects_timer" enabled="y" second="10" offset_second="0.00" send_to="12" active_closed="y"  group="affects_buttons">
    <send>TimerTriggered()</send>
  </timer>

</timers>

<script>

<![CDATA[

require "gmcphelper"

local missing_required_files = {}
local can_update, update_helper = pcall(require, "updatehelper")
local const_installed, consts = pcall(require, "consthelper")
local serializer_installed, serialization_helper = pcall(require, "serializationhelper")
local config_installed, config_window = pcall(require, "configuration_miniwindow")
local buttons_installed, buttons_window = pcall(require, "affectsbuttons_miniwindow")
local badaffects_installed, badaffects_window = pcall(require, "badaffects_miniwindow")

if can_update and update_helper.version == nil then
  update_helper.Update({{ local_file = GetInfo(56):gsub("\\", "/") .. "lua/updatehelper.lua", remote_file = "https://raw.githubusercontent.com/notbilldavis/mm-mush-plugins/refs/heads/main/lua/updatehelper.lua"}})
  package.loaded["updatehelper"] = nil
  can_update, update_helper = pcall(require, "updatehelper")
end

if can_update then
  if not const_installed then const_installed, consts = update_helper.UpdateAndRequire(GetInfo(56):gsub("\\", "/"), "lua/consthelper.lua") end
  if not serializer_installed then serializer_installed, serialization_helper = update_helper.UpdateAndRequire(GetInfo(56):gsub("\\", "/"), "lua/serializationhelper.lua") end
  if not config_installed then config_installed, config_window = update_helper.UpdateAndRequire(GetInfo(56):gsub("\\", "/"), "lua/configuration_miniwindow.lua") end
  if not buttons_installed then buttons_installed, buttons_window = update_helper.UpdateAndRequire(GetInfo(56):gsub("\\", "/"), "lua/affectsbuttons_miniwindow.lua") end
  if not badaffects_installed then badaffects_installed, badaffects_window = update_helper.UpdateAndRequire(GetInfo(56):gsub("\\", "/"), "lua/badaffects_miniwindow.lua") end
end

if not const_installed then table.insert(missing_required_files, "lua/consthelper.lua") end
if not serializer_installed then table.insert(missing_required_files, "lua/serializationhelper.lua") end
if not config_installed then table.insert(missing_required_files, "lua/configuration_miniwindow.lua") end
if not buttons_installed then table.insert(missing_required_files, "lua/affectsbuttons_miniwindow.lua") end
if not badaffects_installed then table.insert(missing_required_files, "lua/badaffects_miniwindow.lua") end

local notify_changes = true
local affects_buffer = {}
local affects_lengths = { name = 6, mod = 8, time = 9, costs = 0 }

function OnPluginInstall() pluginStart() end
function OnPluginEnable() pluginStart() end
function OnPluginConnect() pluginStart() end
function OnPluginClose() pluginStop() end
function OnPluginDisable() pluginStop() end
function OnPluginDisconnect() pluginStop() end

function OnPluginBroadcast(msg, id, name, text)
  if #missing_required_files > 0 then return end
  if (id == "f67c4339ed0591a5b010d05b") then
    if (text == "char.affect_application_msg") then
      handleGmcpAdd()
    elseif (text == "char.affect_removal_msg") then
      handleGmcpRemove()
    elseif (text == "char.base") then
      handleGmcpCharacterName()
    end
  end
end

function pluginStart()
  if #missing_required_files > 0 then
    ColourNote("black", "red", GetPluginName() .. ": You are missing the following required file(s): " .. table.concat(missing_required_files, ", "))
    EnableTriggerGroup("affects_buttons", false)
    EnableAliasGroup("affects_buttons", false)
    EnableTimerGroup("affects_buttons", false)
    return
  else
    EnableTriggerGroup("affects_buttons", true)
    EnableAliasGroup("affects_buttons", true)
    EnableTimerGroup("affects_buttons", true)
  end
  
  EnableTrigger("first_prompt", true)
  EnableTriggerGroup("get_affects", false)
  EnableTriggerGroup("affects_buttons_clean", false)
  buttons_window.Prepare()
end

function pluginStop()
  if #missing_required_files > 0 then return end
  if can_update and buttons_window.IsAutoUpdateEnabled() then
    CheckForUpdates()
  else
    Note("Auto-update for '" .. GetPluginName() .. "' is disabled. Enable it in the plugin configuration to receive updates.")
  end  

  config_window.Hide()
  if not GetInfo(106) then -- not disconnected
    buttons_window.Close()
    badaffects_window.Close()
  end
end

-----------------
--   Aliases   --
-----------------

function AddButton(args)
  local title, affect, command = getCrudArgs(args)
  buttons_window.AddButton(title, affect, command)
end

function EditButton(args)
  local title, affect, command = getCrudArgs(args)
  buttons_window.EditButton(title, affect, command)
end

function DeleteButton(title)
  buttons_window.DeleteButton(title)
end

function RenameButton(args)
  local title, new_title = nil, nil
  if string.find(args, "n:", 1, true) ~= nil then title, new_title = args:match("%s-(.+) n:(.+)%s-") 
  else title = args end
  buttons_window.RenameButton(title, new_title)
end

function getCrudArgs(args)
  if args == nil then args = "" end
  local has_affect, has_command = false, false
  if string.find(args, "a:", 1, true) ~= nil then has_affect = true end
  if string.find(args, "c:", 1, true) ~= nil then has_command = true end
  
  local title, affect, command = nil, nil, nil
  if not has_affect and not has_command then
    title = args
  elseif not has_command then
    title, affect = args:match("%s-(.+) a:(.+)%s-")
  elseif not has_affect then
    title, command = args:match("%s-(.+) c:(.+)%s-")
  else
    title, affect, command = args:match("%s-(.+) a:(.+)%s- c:(.+)%s-")
    if title == nil or affect == nil or command == nil then
      title, command, affect = args:match("%s-(.+) c:(.+)%s- a:(.+)%s-")
    end
  end

  if affect ~= nil then affect = parseAffect(affect) end
  return title, affect, command
end

function MoveButton(args)
  local direction, title = args:match("%s-(%a+) (.+)%s-") 
  if direction == nil or title == nil then 
    Note("Move a button with 'affects move (up|down) <button title>'") 
  else
    if Trim(direction:lower()) == "up" then
      buttons_window.MoveButtonUp(title)
    elseif Trim(direction:lower()) == "down" then
      buttons_window.MoveButtonDown(title)
    else
      Note("Move a button with 'affects move (up|down) <button title>'") 
    end
  end  
end

function Configure(args)
  local which, option, value = args:match("%s-(%a+)%s-(%a-)%s-(%a-)")
  if config_installed then 
    local config = {}
    if which == nil or which:sub(1, 6) == "button" then
      config.Button_Options = buttons_window.GetConfiguration()
    end
    if which == nil or which:sub(1, 8) == "negative" then
      config.Negative_Affects_Options = badaffects_window.GetConfiguration()
    end
    config_window.Show(config, onConfigureDone)
  else
    -- todo
  end  
end

function onConfigureDone(group_id, option_id, config)
  if group_id:find("Negative_Affects") ~= nil then
    badaffects_window.OnConfigureDone(group_id, option_id, config)
  else
    buttons_window.OnConfigureDone(group_id, option_id, config)
  end
end

function Broadcast(args)
  if args == nil or Trim(args) == "" then
    buttons_window.ShowBroadcasts()
  else
    buttons_window.ToggleBroadcastAffect(Trim(args:lower()))
  end
end

function SetCharacterName(name)
  if name == nil or name == "" then
    Note("Unknown character name, set with 'affects setchar <name>'") 
  else
    buttons_window.Initialize(name)
    badaffects_window.Initialize(name)
    DoAfter(1, "affects")
  end
end

function CastFavorites()
  buttons_window.CastFavorites()
end

function CheckForUpdates()
  if can_update then
    update_helper.Update(GetPluginInfo(GetPluginID(), 6):gsub("\\", "/"), "affects_buttons.xml")
    update_helper.Update(GetInfo(56):gsub("\\", "/"), "lua/affectsbuttons_miniwindow.lua")
    update_helper.Update(GetInfo(56):gsub("\\", "/"), "lua/badaffects_miniwindow.lua")
    update_helper.Update(GetInfo(56):gsub("\\", "/"), "lua/configuration_miniwindow.lua")
    update_helper.Update(GetInfo(56):gsub("\\", "/"), "lua/consthelper.lua")
    update_helper.Update(GetInfo(56):gsub("\\", "/"), "lua/serializationhelper.lua")
  else
    ColourNote("red", "black", GetPluginName() .. ": UpdateHelper was not found. Make sure updatehelper.lua is in your lua folder.")
  end
end

----------------
-- Triggers   --
----------------

function OnFirstPrompt()
  Execute("sendgmcp char.base")
end

function StartCapturingAffects()
  notify_changes = false
  EnableTriggerGroup("get_affects", true)
  buttons_window.Clear()
  badaffects_window.Clear()

  local config = buttons_window.GetConfiguration()
  if config.OPTIONS.CLEAN_ENABLED then
    EnableTriggerGroup("affects_buttons_clean", true)
    affects_buffer = {}
  end
end

function StopCapturingAffects()
  notify_changes = true
  EnableTriggerGroup("get_affects", false)
  buttons_window.Draw()
  badaffects_window.Draw()
  EnableTriggerGroup("affects_buttons_clean", false)
  
  local config = buttons_window.GetConfiguration()
  if config.OPTIONS.CLEAN_ENABLED then
    displayCleanAffects()  
  end
end

function CleanAffectLine(line)
  local config = buttons_window.GetConfiguration()
  if config.OPTIONS.CLEAN_ENABLED then
    local typ, name, mod, val, mins = string.match(line, "^(%w+):%s+([%w%s]+)%s+%-%s+Level%s+%d+,%s+modifies%s+([%w%-]+)%s+by%s+([%-%d]+)%s+for%s+(%d+)%s+minutes")
    if not typ then
      typ, name, mod, val = string.match(line, "^(%w+):%s+([%w%s]+)%s+%-%s+Level%s+%d+,%s+modifies%s+([%w%-]+)%s+by%s+([%-%d]+)")
    end

    if not typ or not name then return end
    if typ:lower() == "racial" and config.IGNORE_RACIAL_AFFECTS then return end

    name = Trim(name or "")
    mod = Trim(mod or "")
    val = Trim(val or "")
    mins = Trim(mins or "")

    affects_buffer[name] = affects_buffer[name] or {
      mods = "",
      costs = "",
      costs_raw = {},
      min_time = math.huge,
      timer_string = "permanent",
      timer_color = "green"
    }

    local entry = affects_buffer[name]
    if mod and val then
      entry.mods = entry.mods .. (entry.mods == "" and "" or ", ") .. mod .. " " .. (tonumber(val) >= 0 and "+" or "") .. val
    end

    if #mins > 0 then
      local converted_time = tonumber(mins or 0) or 0
      if converted_time < entry.min_time then
        entry.timer_color, entry.timer_string = getTimerColorAndString(converted_time)
        entry.min_time = converted_time
      end
    end

    for block in string.gmatch(line, "%b[]") do
      local val, stat = string.match(block, "([%-%d%%]+)%s*(%w+)")
      if val and stat then
        if not entry.costs_raw[stat] then
          entry.costs_raw[stat] = val
        end
      end
    end

    entry.costs = ""
    for stat, vals in pairs(entry.costs_raw) do
      entry.costs = entry.costs .. (entry.costs == "" and "" or ", ") .. (vals or "") .. " " .. stat
    end

    if #name > affects_lengths.name then affects_lengths.name = #name end
    if entry.mods and #entry.mods > affects_lengths.mod then affects_lengths.mod = #entry.mods end
    if entry.timer_string and #entry.timer_string > affects_lengths.time then affects_lengths.time = #entry.timer_string end
    if entry.costs and #entry.costs > affects_lengths.costs then affects_lengths.costs = #entry.costs end
  end
end

function SetAffect(affect, time_str, refresh)
  local parsed_affect = parseAffect(affect)
  local converted_time = tonumber(time_str or 0) or 0  
  buttons_window.SetAffect(parsed_affect, converted_time, notify_changes, refresh)
  badaffects_window.SetAffect(parsed_affect, converted_time, refresh)
end

function RecastAffect(affect)
  local parsed_affect = parseAffect(affect)
  local est_duration = buttons_window.GetDuration(parsed_affect)
  if est_duration ~= nil then
    local converted = est_duration / 60 * 4
    local converted_str = tostring(converted)
    SetAffect(affect, converted_str, true)
  end
end

function RemoveAffect(affect)
  local parsed_affect = parseAffect(affect)
  buttons_window.RemoveAffect(parsed_affect, notify_changes)
  badaffects_window.RemoveAffect(parsed_affect, notify_changes)
end

function RemoveAllAffects()
  buttons_window.RemoveAllAffects()
  badaffects_window.RemoveAllAffects()
end

function DisarmedBroadcast(weapon)
  BroadcastPlugin(1, "!!! YOU WERE DISARMED! RE-EQUIP " .. weapon:upper() .. " !!!")
end

function TimerTriggered()
  buttons_window.Draw()
  badaffects_window.Draw()
end

function parseAffect(affect)
  affect = affect:match("^%s*(.-)%s*$"):lower()

  if affect == "ice invocation" or affect == "force field" then
    affect = "shield"

  elseif affect == "ooze invocation" then
    affect = "stone skin"
  
  elseif affect == "steam invocation" or affect == "wind walk" then
    affect = "pass door"

  elseif affect == "magma invocation" or affect == "frostborne" then
    affect = "fireshield"
  
  elseif (affect == "guise of nature") then
    affect = "armor"
  
  elseif affect == "whirlwind" or affect =="lightning invocation" or affect == "zeal" then
    affect = "haste"
  
  elseif affect == "disrupt sight" or affect == "flare" or affect == "color spray" or affect == "smoke evocation" then
    affect = "blindness"
  
  elseif affect == "mesmerize" or affect == "earth evocation" then
    affect = "web"

  elseif affect == "air invocation" or affect == "float" then
    affect = "levitation"

  elseif affect == "irritation" then
    affect = "plague"
  end

  return affect
end

function displayCleanAffects()
  local entries = {}

  for name, data in pairs(affects_buffer) do
    table.insert(entries, {
      name = name,
      data = data
    })
  end

  table.sort(entries, function(a, b)
    local ta = tonumber(a.data.min_time) or math.huge
    local tb = tonumber(b.data.min_time) or math.huge
    if ta == math.huge and tb == math.huge then return a.name < b.name end
    if ta == math.huge then return true end
    if tb == math.huge then return false end
    if ta == tb then return a.name < b.name end
    return ta > tb
  end)

  local total_width = 8 + affects_lengths.name + affects_lengths.mod + affects_lengths.time
  if affects_lengths.costs > 0 then total_width = total_width + affects_lengths.costs + 3 end

  drawHeader(total_width)

  local total_count, perm_count, expiring_count = 0, 0, 0
  for _, affect in pairs(entries) do
    total_count = total_count + 1
    if affect.data.timer_string == "permanent" then perm_count = perm_count + 1
    elseif affect.data.timer_color == "red" then expiring_count = expiring_count + 1 end

    Tell("| ")
    ColourTell("lightgray", "black", affect.name .. string.rep(" ", affects_lengths.name - #affect.name + 1))
    Tell("| ")
    ColourTell("lightgray", "black", affect.data.mods .. string.rep(" ", affects_lengths.mod - #affect.data.mods + 1))
    Tell("| ")
    
    ColourTell(affect.data.timer_color, "black", affect.data.timer_string .. string.rep(" ", affects_lengths.time - #affect.data.timer_string + 1))
    if affects_lengths.costs > 0 then
      Tell("| ")
      ColourTell("lightgray", "black", affect.data.costs .. string.rep(" ", affects_lengths.costs - #affect.data.costs + 1))
    end

    Note("|")
  end

  drawFooter(total_width, total_count, perm_count, expiring_count)
end

function drawHeader(total_width)
  local name_header = "Affect"
  local mod_header = "Modifies"
  local time_header = "Time Left"
  local costs_header = "Costs"

  local name_spacing = string.rep(" ", affects_lengths.name - #name_header)
  local mod_spacing = string.rep(" ", affects_lengths.mod - #mod_header)
  local time_spacing = string.rep(" ", affects_lengths.time - #time_header)
  local costs_spacing = string.rep(" ", affects_lengths.costs - #costs_header)

  Tell(".")
  Tell(string.rep("-", total_width))
  Note(".")

  Tell("| ")
  ColourTell("magenta", "black", name_header .. name_spacing)
  Tell(" | ")
  ColourTell("magenta", "black", mod_header .. mod_spacing)
  Tell(" | ")
  ColourTell("magenta", "black", time_header .. time_spacing)
  if (affects_lengths.costs > 0) then
    Tell(" | ")
    ColourTell("magenta", "black", costs_header .. costs_spacing)
  end
  Note(" |")

  Tell(".")
  Tell(string.rep("-", affects_lengths.name + 2))
  Tell(".")
  Tell(string.rep("-", affects_lengths.mod + 2))
  Tell(".")
  Tell(string.rep("-", affects_lengths.time + 2))
  if (affects_lengths.costs > 0) then
    Tell(".")
    Tell(string.rep("-", affects_lengths.costs + 2))
  end
  Note(".")
end

function drawFooter(total_width, total_count, perm_count, expiring_count)
  Tell(".")
  Tell(string.rep("-", affects_lengths.name + 2))
  Tell(".")
  Tell(string.rep("-", affects_lengths.mod + 2))
  Tell(".")
  Tell(string.rep("-", affects_lengths.time + 2))
  if (affects_lengths.costs > 0) then
    Tell(".")
    Tell(string.rep("-", affects_lengths.costs + 2))
  end
  Note(".")  

  local summary = string.format("Total: %d", total_count)
  if perm_count > 0 then summary = summary .. string.format(", Permanent: %d", perm_count) end
  if expiring_count > 0 then summary = summary .. string.format(", Expiring Soon: %d", expiring_count) end

  local padding = string.rep(" ", total_width - #summary - 2)
  Tell("| ")
  ColourTell("cyan", "black", summary .. padding)
  Note(" |")

  Tell(".")
  Tell(string.rep("-", total_width))
  Note(".")
end

getTimerColorAndString = function(time)
  if (time == nil or time == math.huge) then
    return "green", "permanent"
  else
    time = time / 4 * 60 -- convert from mud time to real time
    local seconds = time % 60
    local minutes = math.floor(time / 60) % 60
    local hours = math.floor(time / 3600) % 24

    local color = "green"
    if (minutes < 5) then
      color = "red"
    elseif minutes < 10 then
      color = "yellow"
    end

    if hours > 0 then
      return color, string.format("%02d hr, %02d min, %02d sec", hours, minutes, seconds)
    end
    
    return color, string.format("%02d min, %02d sec", minutes, seconds)
  end
end

---------------
-- GMCP stuff
---------------

function handleGmcpAdd()
  local gmcp_value = getGmcpValue("char.affect_application_msg")
  if gmcp_value.target == "you" then
    SetAffect(gmcp_value.skill:lower(), tonumber(gmcp_value.duration or 0), true)
  end
end

function handleGmcpRemove()
  local gmcp_value = getGmcpValue("char.affect_application_msg")
  if gmcp_value.target == "you" then
    RemoveAffect(gmcp_value.skill:lower())
  end
end

function handleGmcpCharacterName()
  if GetTriggerInfo("first_prompt", 8) then
    EnableTrigger("first_prompt", false)
    local gmcp_value = getGmcpValue("char.base")
    SetCharacterName(gmcp_value.name)
  end
end

function getGmcpValue(field)
  local res, value = CallPlugin("f67c4339ed0591a5b010d05b", "gmcpval", field)
  local func = load("return " .. value)
  if func then return func()
  else return nil end
end

]]>

</script>

</muclient>
